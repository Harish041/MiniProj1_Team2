Create table Student(
ID int primary key not null,
Name varchar(30),
dob date)

Create table Course(
cid int primary key not null,
name varchar(20),
duration varchar(10),
fee numeric(10,2))



Select*from Course